# Default values — dev environment baseline
replicaCount: 1
namespace: medicare-dev

image:
  repository: nginx
  tag: "1.26"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

resources:
  limits:
    cpu: 250m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
